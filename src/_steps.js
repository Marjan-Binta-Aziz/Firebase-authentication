/**
 * Steps to use fire box
 * 1. Create a project on console.fire.google.com
 * 2. install firebox "npm install firebase"
 * 3. Register Web app in firebase 
 * 4. copy firebase init with config from propjets settings
 * 5. export default app
 * 6. import app firebase init.js into my app.js
 * 7. import getAuth from firebase/auth & create auth = getAuth(app)
 * 8. turn on authentication (firebase> authentication > enable google sign in)
 * 9. creat google provider
 * 10. use signInWithPopup nd pass auth asd provider 
 * 11. handle .then (successful) nd catch error (if error) 


*/
